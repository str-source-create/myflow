/**
 * StatusBadge.jsx
 * Re-exports the shared admin StatusBadge so both admin and worker views
 * use identical status colours and labels.
 */
export { default } from '../../components/StatusBadge'

